#include <bits/stdc++.h>
using namespace std;

int main (){
      
      int n,q;
      cin>>n>>q;
      vector<int>s(n,0);
      for(int i=0;i<n;i++)cin>>s[i];
     // atleast in nlogn
      while (q--){
           
             int c;
             cin>>c;
             
      }
}