#include <iostream>
#include <vector>
using namespace std;

long long solve(vector<long long >&s, int n){
          
}

int main (){
      ios::sync_with_stdio(false);
    
      long long n ;
      cin>>n;

      vector<long long >s;
      for(int i=0;i<n;i++){
          cin>>s[i];
      }

      return solve(s,n);


}

// 